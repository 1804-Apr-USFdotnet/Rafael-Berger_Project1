@model ResterauntWeb.ViewModels.RestaurantVm
<table>
  
@foreach (var rest in Model.SearchResults)
{
    
    <tr style="border:2px solid black">
       <td>@rest.Name</td>

        <td>@Html.ActionLink("Details", "Details", new { id = rest.Id })</td>
    </tr>
}

</table>